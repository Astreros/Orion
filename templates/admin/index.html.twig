{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #38D5BE; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Accueil administration ✅</h1>

        {% if app.user %}
            <a href="{{ path('app_logout') }}">Déconnexion</a>
        {% endif %}

        <br>
        <br>

        <a href="{{ path('show.qrcode') }}">Administration des QR Code</a>
        <br>

        <a href="{{ path('show.code') }}">Administration des Code PIN</a>
        <br>

        <a href="{{ path('show.badge') }}">Administration des Badge</a>
        <br>

        <a href="{{ path('show.user') }}">Administration des Utilisateurs</a>
    </div>
{% endblock %}
